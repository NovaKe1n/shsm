#!/usr/bin/env bash

#title|img
#version|1.0
#author|NovaKe1n
#info|open an image with preview

if [[ "$1" ]]; then
    printf "${Green}Opening:${NC} $1 ${Green}on preview...\n"
    open -a "preview" "$1"
else
    printf "${Red}Give me the img!\n"
fi
